// The app no longer uses the SwiftUI App lifecycle. Compose is hosted directly as the
// window's root view controller via AppDelegate / SceneDelegate (see AppDelegate.swift),
// which avoids the SwiftUI safe-area inset compounding on rotation.
